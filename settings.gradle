pluginManagement {
    repositories {
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

if (Boolean.parseBoolean(System.getProperty("reden.testmod"))) {
    include("testmod")
}
